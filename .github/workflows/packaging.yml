name: Bundling and Packaging

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release. Must follow the format of 'X.Y.Z'."
        required: true

jobs:
  setup:
    name: "Define variables and configure environment"
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.define-version.outputs.version }}
    steps:
      - name: Define version
        id: define-version
        env:
          VERSION: ${{ inputs.version }}
        run: |
          if [[ ! "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "VERSION must follow the format of 'X.Y.Z'. Value: '$VERSION'."
            exit 2
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT

  bundle:
    name: "Bundling for ${{ matrix.platform.os-display }} (${{ matrix.platform.arch }})"
    needs: setup
    env:
      VERSION: ${{ needs.setup.outputs.version }}
    strategy:
      fail-fast: false
      matrix:
        platform:
          - os: linux
            os-display: Linux
            arch: x86_64
            builder: ubuntu-24.04
            target_triple: x86_64-unknown-linux-gnu
          - os: macos
            os-display: MacOS
            arch: aarch64
            builder: macos-14
            target_triple: aarch64-apple-darwin

    runs-on: ${{ matrix.platform.builder }}
    steps:
      - name: Checkout
        uses: actions/checkout@85e6279cec87321a52edac9c87bce653a07cf6c2  # Version 4.2.2

      - name: Add the target toolchain
        run: rustup target add ${{ matrix.platform.target_triple }}

      - name: Install dependencies
        uses: ./.github/actions/install-bundling-dependencies
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          os: ${{ matrix.platform.os }}
          target_triple: ${{ matrix.platform.target_triple }}

      - name: Setup Circuits
        uses: ./.github/actions/setup-circuits
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions-rs/cargo@9e120dd99b0fbad1c065f686657e914e76bd7b72  # Version 1.0.1
        name: Build Logos blockchain node
        with:
          command: build
          args: --package logos-blockchain-node --release --no-default-features --target ${{ matrix.platform.target_triple }}

      - uses: actions-rs/cargo@9e120dd99b0fbad1c065f686657e914e76bd7b72  # Version 1.0.1
        name: Build Logos blockchain node bundler
        with:
          command: build
          args: --bin logos-blockchain-bundle-node --release --target ${{ matrix.platform.target_triple }}

      - name: Bundle
        shell: bash
        env:
          RUST_LOG: info
        run: |
          # Define version parameter for the binary
          PARAMETER="-v ${{ env.VERSION }}"

          if [[ ${{ github.event.debug || false }} == true ]]; then
            echo "This workflow is on debug mode: the bundle command's `-v` parameter will be removed for debugging." 
            PARAMETER=""
          fi
  
          "target/${{ matrix.platform.target_triple }}/release/logos-blockchain-bundle-node" $PARAMETER

      - name: Flatten Linux bundles
        if: matrix.platform.os == 'linux'
        run: |
          mv target/${{ matrix.platform.target_triple }}/release/bundle/rpm/logos-blockchain-node-${{ env.VERSION }}_x86_64.rpm logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.rpm
          mv target/${{ matrix.platform.target_triple }}/release/bundle/deb/logos-blockchain-node_${{ env.VERSION }}_amd64.deb logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.deb
          mv target/${{ matrix.platform.target_triple }}/release/bundle/appimage/logos-blockchain-node_${{ env.VERSION }}_amd64.AppImage logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.AppImage

      - name: Upload RPM (${{ matrix.platform.arch }})
        if: matrix.platform.os == 'linux'
        uses: actions/upload-artifact@6027e3dd177782cd8ab9af838c04fd81a07f1d47 # Version 4.6.2
        with:
          name: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.rpm
          path: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.rpm
          if-no-files-found: error

      - name: Upload DEB (${{ matrix.platform.arch }})
        if: matrix.platform.os == 'linux'
        uses: actions/upload-artifact@6027e3dd177782cd8ab9af838c04fd81a07f1d47 # Version 4.6.2
        with:
          name: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.deb
          path: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.deb
          if-no-files-found: error

      - name: Upload AppImage (${{ matrix.platform.arch }})
        if: matrix.platform.os == 'linux'
        uses: actions/upload-artifact@6027e3dd177782cd8ab9af838c04fd81a07f1d47 # Version 4.6.2
        with:
          name: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.AppImage
          path: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.AppImage
          if-no-files-found: error

      - name: Flatten MacOS bundles
        if: matrix.platform.os == 'macos'
        run: |
          mv target/${{ matrix.platform.target_triple }}/release/bundle/dmg/logos-blockchain-node_${{ env.VERSION }}_${{ matrix.platform.arch }}.dmg logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.dmg

      - name: Upload DMG (${{ matrix.platform.arch }})
        if: matrix.platform.os == 'macos'
        uses: actions/upload-artifact@6027e3dd177782cd8ab9af838c04fd81a07f1d47 # Version 4.6.2
        with:
          name: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.dmg
          path: logos_blockchain_node-${{ env.VERSION }}-${{ matrix.platform.arch }}.dmg
          if-no-files-found: error

  create-release:
    name: Create release
    needs:
      - setup
      - bundle
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.setup.outputs.version }}
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Create Release
        uses: actions/create-release@4c11c9fe1dcd9636620a16455165783b20fc7ea0  # Version 1.1.4
        id: create_release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ env.VERSION }}
          release_name: Logos Blockchain Node ${{ env.VERSION }}
          body: |
            Logos Blockchain Node ${{ env.VERSION }}.

            ## Changelog
            - feature(X): new feature
            - fix(Y): bug description
            - feature: performance improvement on Z
            ## Checklist
            Before publishing please ensure:
            - [ ] Description is complete
            - [ ] Changelog is correct
            - [ ] Assets for all platforms exist
            - [ ] Circuits are added
            - [ ] Pre-release is checked if necessary
            - [ ] Remove this checklist before publishing the release.
          draft: true
          prerelease: true

  upload-artifacts:
    name: Upload Artifacts to release
    needs:
      - setup
      - create-release
    runs-on: ubuntu-latest
    strategy:
      matrix:
        platform:
          - os: linux
            arch: x86_64
            extension: deb
            extension-display: DEB
          - os: linux
            arch: x86_64
            extension: rpm
            extension-display: RPM
          - os: linux
            arch: x86_64
            extension: AppImage
            extension-display: AppImage
          - os: macos
            arch: aarch64
            extension: dmg
            extension-display: DMG

    env:
      UPLOAD_URL: ${{ needs.create-release.outputs.upload_url }}
      BINARY_NAME: logos_blockchain_node-${{ needs.setup.outputs.version }}-${{ matrix.platform.arch }}.${{ matrix.platform.extension }}
      DOWNLOAD_DIRECTORY: artifacts
    steps:
      - name: Download Artifact
        uses: actions/download-artifact@95815c38cf2ff2164869cbab79da8d1f422bc89e # Version 4.2.1
        with:
          name: ${{ env.BINARY_NAME }}
          path: ${{ env.DOWNLOAD_DIRECTORY }}
          merge-multiple: 'true'

      - name: Upload ${{ matrix.platform.extension-display }} (${{ matrix.platform.arch }})
        uses: actions/upload-release-asset@ef2adfe8cb8ebfa540930c452c576b3819990faa  # Version 1.0.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ env.UPLOAD_URL }}
          asset_path: ${{ env.DOWNLOAD_DIRECTORY }}/${{ env.BINARY_NAME }}
          asset_name: ${{ env.BINARY_NAME }}
          asset_content_type: application/octet-stream
