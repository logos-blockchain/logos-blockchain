# Logrotate configuration for Logos Blockchain Node
#
# OPTIONAL: Only needed if you enabled file logging in the systemd service
# (StandardOutput/StandardError lines uncommented)
#
# To install:
#   sudo cp logrotate-logos-node.conf /etc/logrotate.d/logos-node
#   sudo nano /etc/logrotate.d/logos-node
#   # Replace <username> with your actual username

/home/<username>/logos-node.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
